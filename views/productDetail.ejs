<!DOCTYPE html>
<html>
<head>
   <link rel="icon" type="image/png" href="/favicon.png">
  <title>💻 MyShop</title>
  <style>
    :root { --bg: #f6f8fb; --card: #ffffff; --accent: #007bff; --accent-dark: #0056b3; --muted: #6b7280; --border: #e5e7eb; }
    * { box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif; background: linear-gradient(135deg, var(--bg) 0%, #f0f4f8 100%); margin: 0; min-height: 100vh; }
    .container { max-width: 900px; margin: 40px auto; padding: 24px; }
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
    .logo { height: 36px; display: block; }
    .center-logo-wrap { text-align: center; margin-bottom: 8px; }
    .topbar { display:flex; justify-content:space-between; align-items:center; padding: 16px 24px; background: transparent; }
    .topbar .brand { display:flex; align-items:center; gap:12px; }
    .site-title { font-size:18px; font-weight:700; color:#0f172a; }
    .back-link { text-decoration: none; color: var(--accent); font-weight: 600; font-size: 15px; transition: all 0.2s; display: inline-flex; align-items: center; gap: 6px; }
    .back-link:hover { color: var(--accent-dark); transform: translateX(-4px); }
    .content { background: var(--card); border-radius: 16px; padding: 32px; box-shadow: 0 20px 60px rgba(15,23,42,0.1); overflow: hidden; }
    .product-image { width: 100%; height: 300px; object-fit: contain; object-position: center; border-radius: 12px; margin-bottom: 32px; background-color: #ffffff; }
    h1 { margin: 0 0 16px 0; font-size: 36px; font-weight: 700; color: #0f172a; letter-spacing: -0.5px; }
    .price { font-weight: 800; font-size: 28px; color: var(--accent); margin: 24px 0; }
    .description { color: #374151; font-size: 16px; line-height: 1.8; margin-bottom: 32px; }
    .button-group { display: flex; gap: 12px; }
    .btn { padding: 12px 24px; border-radius: 10px; font-weight: 600; font-size: 15px; border: none; cursor: pointer; transition: all 0.2s; text-decoration: none; display: inline-block; }
    .btn-primary { background: var(--accent); color: #fff; }
    .btn-primary:hover { background: var(--accent-dark); transform: translateY(-2px); box-shadow: 0 6px 18px rgba(0,123,255,0.3); }
    .btn-secondary { background: transparent; color: var(--accent); border: 2px solid var(--border); }
    .btn-secondary:hover { background: #fff; border-color: var(--accent); transform: translateY(-2px); box-shadow: 0 6px 18px rgba(15,23,42,0.08); }
    @media (max-width: 768px) { .container { padding: 16px; margin: 24px auto; } .content { padding: 24px; } .product-image { height: 300px; } h1 { font-size: 28px; } .price { font-size: 24px; } .button-group { flex-direction: column; } 
}
 
</style>
</head>
<body>
  <div class="container">
    <div class="topbar">
      <div class="brand"><img src="/logo.png" alt="Logo" class="logo"><span class="site-title">MyShop</span></div>
      <a class="back-link" href="/products">← Back to products</a>
    </div>
    <div class="header"></div>
    <div class="content">
      <img src="/download.jpg" alt="<%= product.name %>" class="product-image">
      <h1><%= product.name %></h1>
      <div class="price">$<%= product.price %></div>
      <p class="description"><%= product.description || 'High quality product' %></p>
      <div class="button-group">
        <button class="btn btn-primary">Add to Cart</button>
        <a href="/products" class="btn btn-secondary">Continue Shopping</a>
      </div>
    </div>
  </div>
</body>
</html>
